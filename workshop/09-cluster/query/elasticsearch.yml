node.name: query
cluster.name: demo
node.master: false
node.data: false
node.ingest: false
cluster.initial_master_nodes: localhost

bootstrap.memory_lock: true

path:
    data: $ES_PATH/query
    logs: $ES_PATH/query
